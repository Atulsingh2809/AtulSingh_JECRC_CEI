# Model Artifacts

Trained model files (`.pkl`, `.joblib`) are saved here after running `src/train.py`.

These files are **gitignored** by default. To reproduce:

```bash
python -m src.train
```

The primary deployed model is **Model A** (pre-sales-contact features only, no data leakage).
